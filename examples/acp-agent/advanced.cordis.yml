# Add Code Mode and Cordis tools to the base spawn/workflow stack, exercising
# all four boundaries in one ACP snapshot.
- id: base
  name: '@harness-desktop/cordis-plugin-include'
  config:
    path: ./cordis.yml
    patches:
      - id: acp-agent
        name: '@harness-desktop/dsh-acp-demo'
        config:
          harnessHome: !!js harnessHome
          provider: deepseek-official
          model: deepseek-v4-pro
          persistenceRoot: !!js process.env.DSH_SNAPSHOT_SESSIONS_ROOT ?? harnessHomePath('sessions')
          persistenceCompression: !!js "process.env.DSH_SNAPSHOT === undefined ? 'zstd' : 'none'"
          workspaceContext:
            maxBytes: 65536
          tools:
            mode: both
          persona: |
            You are a coding assistant powered by the {{model}} model. Your working directory is {{cwd}}.

            Verify your work by running the code or tests. Keep answers brief and factual.
      - insert:
          - id: code-runtime
            name: '@harness-desktop/dsh-code-runtime-worker-thread'
          - id: cordis-host-runner
            name: '@harness-desktop/dsh-cordis-host-runner'
          - id: tool-cordis
            name: '@harness-desktop/dsh-tool-cordis'
