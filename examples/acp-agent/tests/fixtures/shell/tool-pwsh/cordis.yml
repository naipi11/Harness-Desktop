# Minimal tool-pwsh composition: real app boot path, real pwsh executor, real
# foreground + background tool calls; driven by the package's loader.spec.ts.
- id: system-prompt
  name: '@harness-desktop/dsh-system-prompt'

- id: tools
  name: '@harness-desktop/dsh-tools'

- id: subprocess
  name: '@harness-desktop/dsh-subprocess-local'

- id: bash
  name: '@harness-desktop/dsh-pwsh-local'
  config:
    graceMs: 200

- id: shell-env
  name: '@harness-desktop/dsh-shell-env'
  config:
    harnessHome: !!js harnessHome

- id: tasks
  name: '@harness-desktop/dsh-jobs-local'

- id: tool-jobs
  name: '@harness-desktop/dsh-tool-jobs'

- id: tool-pwsh
  name: '@harness-desktop/dsh-tool-pwsh'
