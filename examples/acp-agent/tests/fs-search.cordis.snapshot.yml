# Minimal keyless composition: real app, bash, and search tool; replayed model.
- id: llm-replay
  name: '@stackstackstack/dsh-llm-replay'
  config:
    providers:
      - id: deepseek-official
        name: DeepSeek
        models:
          - id: deepseek-v4-pro

- id: subprocess
  name: '@stackstackstack/dsh-subprocess-local'

- id: bash
  name: '@stackstackstack/dsh-bash-local'

- id: acp-agent
  name: '@stackstackstack/dsh-acp-demo'
  config:
    provider: deepseek-official
    model: deepseek-v4-pro
    persistenceRoot: !!js process.env.DSH_SNAPSHOT_SESSIONS_ROOT ?? './.sessions'
    persistenceCompression: none
    workspaceContext: false
    skills:
      enabled: false
    toolJobs: false
    goals: false
    persona: You are a concise snapshot agent working in {{cwd}}.

- id: tool-fs-search
  name: '@stackstackstack/dsh-tool-fs-search'
  config:
    sampleOverCapGlobResults: true
    globMaxResults: 4
