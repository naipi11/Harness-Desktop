# Test-only composition: keep time-context opt-in while exercising its real Loader/app path.
- id: time-context-mock-llm
  name: './time-context-mock-llm.ts'

# Managed child-process groups for the bash executor (spawn/kill/output plumbing).
- id: subprocess
  name: '@stackstackstack/dsh-subprocess-local'

- id: bash
  name: '@stackstackstack/dsh-bash-local'

- id: time-context
  name: '@stackstackstack/dsh-time-context'

- id: agent-spine
  name: '@stackstackstack/dsh-agent-spine-demo'
  config:
    agents:
      - id: main
        provider: time-context-mock
        model: time-context-mock
        cwd: !!js process.cwd()
    persona: 'Test the time-context plugin.'
    workspaceContext: false

- id: persistence
  name: '@stackstackstack/dsh-session-persistence-jsonl'
  config:
    root: './.sessions'
    compression: 'none'

- id: checkpoint-policy
  name: '@stackstackstack/dsh-session-checkpoint-policy'
